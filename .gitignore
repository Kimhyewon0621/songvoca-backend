# Node.js dependencies — recreated by `npm install`
node_modules/

# Environment variables — contains JWT_SECRET, must never be committed
.env

# Test coverage reports — regenerated by `npm run test:coverage`
coverage/

# Editor / OS junk
.DS_Store
.vscode/
*.log